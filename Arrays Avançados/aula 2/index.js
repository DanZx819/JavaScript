//

const nomes = new Array('Eduardo', 'Maria', 'Joana', 'Cleiton', 'Julia', 'Sofia'); // Construtor

// nomes.slice(indice, delete, elem1, elem2, elem3);
// pop

// const removidos = nomes.splice(-2, Number.MAX_VALUE);

// const adicionados = nomes.splice(0, 2, 'Daniel', 'Colonhezi');

// const pop = nomes.splice(-1, 1);

//const shift = nomes.splice(0, 1);

//const unshift = nomes.splice(0, 0, 'Luiz');

// const push = nomes.splice(nomes.length, 1, 'Luiz', 'Otávio');

console.log(nomes, push);